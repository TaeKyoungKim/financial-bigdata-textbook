# Chapter 01. 금융 빅데이터와 머신러닝 생태계의 이해

> 금융 머신러닝 입문 · 개정 1.0 · 개념 → 수식 → 실습 → 검증 → 해석

금융 머신러닝은 시장의 정답을 외우는 작업이 아니다. 예측 시점에 이용할 수 있었던 데이터에서 관계를 추정하고, 새로운 시기에 그 관계가 유지되는지 검증하는 과정이다. 좋은 모델을 만들려면 알고리즘에 앞서 데이터의 의미, 시간 순서, 거래비용과 평가 기준을 정확히 정해야 한다.

이 장은 첨부 자료 *Machine Learning with Python*의 Chapter 01(PDF 파일 기준 3–67쪽)을 금융 시계열 관점으로 확장한 것이다. 사용자 초안의 네 가지 주제는 유지하되, 수식과 코드의 불일치 및 검증되지 않은 성과 주장은 수정했다. 본문의 그림은 이 장을 위해 새로 작성한 개념도와 합성 데이터 실험이다. 실제 시장·펀드의 실적을 나타내지 않는다.

## 이 장의 학습 목표

1. 규칙 기반 시스템과 학습 기반 시스템의 역할을 구분한다.
2. 작은 예측 우위에 거래비용과 상관관계가 미치는 영향을 계산한다.
3. 비정상성, 정보 누수, 생존편향을 구분한다.
4. 액면분할을 보정하고 피처와 미래 타깃을 정렬한다.
5. 파이썬 패키지의 역할과 다음 장의 학습·검증 흐름을 설명한다.

**선수 지식**: 파이썬 함수와 배열, 평균·분산, 로그의 기본 성질. 마르코프 전환 모형과 ADF 검정은 심화 내용이므로 첫 독서에서는 의미를 먼저 이해해도 된다.

## 1.0 먼저 잡아야 할 개념: 무엇을 학습하는가

원본 PDF의 피처·레이블·학습과 예측 설명(35–49쪽)을 금융 데이터에 옮기면 다음과 같다.

| 용어 | 의미 | 금융 예시 |
|---|---|---|
| 샘플 | 한 번의 관측 단위 | 특정 종목의 특정 거래일 |
| 피처 X | 예측 시점에 알려진 입력 | 과거 20일 모멘텀과 변동성 |
| 타깃 y | 예측하려는 결과 | 이후 5일 누적 수익률 |
| 학습 | 과거 표본에서 관계·파라미터 추정 | 모멘텀과 미래 수익률의 관계 추정 |
| 추론 | 학습된 모델을 새 입력에 적용 | 오늘 데이터로 이후 수익률 예측 |
| 일반화 | 새 표본에서 유용성이 유지되는 성질 | 미래 검증 구간에서도 기준 모형보다 개선 |

지도학습의 **회귀**는 수익률처럼 연속적인 값을, **분류**는 상승·비상승처럼 정해진 범주를 예측한다. 비지도학습은 정답 라벨 없이 비슷한 종목을 묶거나 변수의 구조를 압축한다. 강화학습은 행동과 보상의 상호작용을 다루지만, 금융에서는 시장 시뮬레이터와 거래 제약의 정확성이 별도로 필요하다. 방법의 복잡도가 수익성을 보장하지 않는다.

인공지능은 더 넓은 개념이고, 머신러닝은 그 안에서 데이터로 관계를 추정하는 방법들의 집합이다. 딥러닝은 여러 층의 신경망을 사용하는 머신러닝의 한 갈래다. 학습 기반 시스템도 데이터 처리, 학습 목표, 제약 조건을 정의하는 프로그램이 필요하다.

## 1.1 규칙 기반 시스템의 한계와 금융 조합 폭발

### 1.1.1 금융 비즈니스와 이론

규칙 기반 시스템은 사람이 결정 조건을 명시한다. 예를 들어 금리·환율·수급에 임계값을 정하고 조건이 맞으면 포지션을 보유한다. 조건이 단순하고 책임 소재가 분명해야 하는 업무에서는 유용하다. 주문 한도, 거래 금지 종목, 손실 제한과 같은 통제도 규칙으로 구현할 수 있다.

문제는 모든 시장 상태를 예외 규칙으로 설명하려 할 때 생긴다. 변수와 구간이 늘어나면 검토할 조합이 급증한다. 같은 수급 신호라도 시장의 유동성이나 참여자가 바뀌면 미래 수익률과의 관계가 달라질 수 있다. 이런 관계 변화는 규칙 기반 시스템뿐 아니라 학습된 모델에도 영향을 준다. 머신러닝은 자동 적응을 보장하는 대체품이 아니라 관계를 추정하고 갱신할 수 있는 도구다.

### 1.1.2 수식과 가정

D개 변수를 각각 K개 구간으로 나눌 때 전체 격자의 상태 수는 다음과 같다.

$$|\Omega| = K^D$$

각 셀에 평균 m개의 표본을 배치하려는 균등한 격자 설계에서는 대략 mKᴰ개의 표본이 필요하다. 이는 **격자를 빠짐없이 채우려는 설계의 부담**이지, 모든 학습 방법에 적용되는 보편적 최소 표본 수 하한은 아니다. 실제 필요한 표본 수는 분포, 모델의 가정, 희소성, 허용 오차에 따라 달라진다.

잠재 시장 상태를 Sₜ라고 하면 관계 변화의 한 예를 다음과 같이 쓸 수 있다.

$$y_t = \mu_{S_t} + \sum_{i=1}^{p}\phi_{i,S_t}y_{t-i}+\epsilon_t,\qquad \epsilon_t\mid S_t\sim N(0,\sigma_{S_t}^2)$$

$$P(S_t=j\mid S_{t-1}=i)=p_{ij},\qquad \sum_j p_{ij}=1$$

이는 마르코프 전환 모형의 한 형태다. 상태별 평균·분산·자기회귀 계수가 달라질 수 있다는 뜻이며, 상태가 바뀌면 오차가 반드시 발산한다는 뜻은 아니다. 아래 실습은 이 모형을 추정하지 않고, 관계의 부호가 바뀌는 상황을 직접 가정한다.

![그림 1-1. 조합 증가와 관계 변화](assets/fig_01_01.png)

*그림 1-1. 왼쪽은 변수별 3구간 분할의 조합 수, 오른쪽은 합성 시장에서 고정 신호를 적용한 결과다. 후반부 관계의 반전은 실험 설계에 포함되어 있다.*

### 1.1.3 재현 가능한 실습

신호 xₜ는 t일 종료 후 관측하며, t+1일 수익률에 적용한다. 거래비용은 포지션 변화량에 비례하는 단순 가정이다. 단순 수익률을 사용하므로 자산은 (1 + 전략 수익률)의 누적곱으로 계산한다. 현금 이자와 최종 청산 비용은 생략했다. 현실의 실행 가격과 거래 시점은 별도로 모델링해야 한다.

```python
import numpy as np
import pandas as pd

def regime_example(n=1000, seed=42, cost=0.0003):
    """Known signal flips sign halfway; this is an assumed scenario."""
    rng = np.random.default_rng(seed)
    x = rng.normal(size=n)
    beta = np.where(np.arange(n) < n // 2, 0.003, -0.003)
    # x[t-1] predicts return[t]; x[t] only becomes available after return[t].
    lag_x = np.r_[0.0, x[:-1]]
    asset_return = beta * lag_x + rng.normal(0, 0.012, n)
    signal = (x > 0).astype(float)
    held = np.r_[0.0, signal[:-1]]
    turnover = np.abs(np.diff(np.r_[0.0, held]))
    net = held * asset_return - cost * turnover
    equity = np.r_[1.0, np.cumprod(1 + net)]
    peak = np.maximum.accumulate(equity)
    drawdown = equity / peak - 1
    return pd.DataFrame({'return': asset_return, 'signal': signal,
                         'held': held, 'net': net}), equity, drawdown

market, equity, drawdown = regime_example()
regime_result = {}
for name, sample in [('전반', market.iloc[:500]), ('후반', market.iloc[500:])]:
    wealth = np.r_[1, np.cumprod(1 + sample['net'])]
    regime_result[name] = {'누적수익률': float(wealth[-1] - 1),
        '최대낙폭': float(-(wealth / np.maximum.accumulate(wealth) - 1).min())}
```

### 1.1.4 실제 실행 결과와 해석

| 구간 | 비용 후 누적 수익률 | 구간 내 최대낙폭 |
|---|---:|---:|
| 전반 500일 | 49.40% | 9.74% |
| 후반 500일 | -71.54% | 72.68% |


후반의 성과 변화는 이 데이터 생성 과정의 결과다. 이것만으로 모든 규칙 기반 전략이 실패하거나 머신러닝이 우월하다고 결론 내릴 수 없다. 비교하려면 같은 데이터, 같은 비용, 같은 위험 제약에서 학습 모델과 기준 전략을 평가해야 한다.

최대낙폭은 각 시점 자산을 그때까지의 고점으로 나눈 값에서 계산한다. 위 표의 낙폭은 구간별 초기자산을 1로 놓고 계산했다.

$$V_t=\prod_{u=1}^{t}(1+R_u^{net}),\quad H_t=\max_{0\le u\le t}V_u,\quad MDD=-\min_t(V_t/H_t-1),\quad V_0=1$$

로그 수익률에 포지션을 곱한 뒤 지수화하는 방식은 일반적인 레버리지·현금 혼합 포트폴리오의 정확한 회계식이 아니다. 자산 로그 수익률이 rₜ라면 먼저 단순 수익률 exp(rₜ)−1로 바꾸어 포지션과 비용을 적용한다.

## 1.2 전문가 시스템에서 데이터 주도형 퀀트로

### 1.2.1 금융 비즈니스와 이론

규칙 기반 접근은 사람이 관계를 정하고, 학습 기반 접근은 데이터를 사용해 관계의 파라미터를 추정한다. 실제 운용 시스템은 두 방법을 함께 쓸 수 있다. 예측은 모델이 담당하고 주문 크기와 허용 자산은 규칙이 제한하는 방식이다.

데이터 주도형 연구의 핵심은 특정 유명 펀드의 비공개 전략을 재현하는 데 있지 않다. 작은 통계적 관계가 새로운 표본에서도 유지되는지, 거래비용을 넘는지, 충분히 독립적인 기회가 있는지를 검증하는 데 있다. 초안의 특정 펀드 승률과 무위험 수렴 주장은 확인 가능한 근거가 없어 일반적인 가정 실험으로 대체했다.

### 1.2.2 작은 우위의 수학: 비용과 상관관계

승리 확률 p, 이길 때 +b, 질 때 −b, 매 거래의 고정 비용 c를 가정한다. R은 거래 규모에 대한 손익 비율이다.

$$E[R]=(2p-1)b-c,\qquad Var(R)=4p(1-p)b^2$$

p=0.508, b=0.0015이면 비용 전 기대손익은 0.000024, 즉 **0.24bps**다. 1bps는 0.01%포인트이므로 0.24bps는 0.0024%다. 이 모형에서는 거래당 총비용이 이 수준을 넘으면 기대손익이 음수가 된다. 비용은 연구 결과의 부수적인 항목이 아니다. 실제 비용에는 수수료 외에도 스프레드, 시장 충격 등이 포함될 수 있다. [참고: SEC 투자자 교육 자료](https://www.sec.gov/oiea/investor-alerts-bulletins/investoralertsib_fees_expensespdf).

동일 분산 σ²의 거래 N개가 독립이면 합계 손익 Sₙ의 분산은 Nσ²다. 모든 거래쌍의 상관계수가 동일하게 ρ라고 가정하면 다음과 같다.

$$Var(S_N)=N\sigma^2[1+(N-1)\rho]$$

$$N_{eff}=\frac{N}{1+(N-1)\rho}$$

이는 균일 상관 가정에서의 민감도 계산이다. 예를 들어 N=10,000, ρ=0.01이면 유효 독립 표본 수는 약 99개다. 주문 횟수가 늘어도 독립적인 정보가 그만큼 늘지는 않는다.

독립·동일 분포 가정에서 E[Sₙ]/SD(Sₙ)는 √N에 비례하지만, 이 비율을 바로 연율화 샤프 지수라고 부를 수는 없다. 샤프 지수는 수익률의 기간, 무위험 수익률, 연율화 조건을 명시해야 한다. 능동 운용의 근사 관계 IR ≈ IC√BR에서도 BR은 명목 주문 수가 아닌 독립적 의사결정 기회의 폭으로 해석한다. 실제 포트폴리오 제약과 상관관계는 이 단순화에서 빠져 있다.

![그림 1-2. 거래비용과 독립성](assets/fig_01_02.png)

*그림 1-2. 승률이 절반을 조금 넘더라도 비용 후 기대손익은 음수가 될 수 있다. 오른쪽은 일정한 양의 상관관계가 독립 기회 수를 제한하는 예다.*

### 1.2.3 재현 가능한 실습

```python
import numpy as np
import pandas as pd

def edge_example(p=0.508, b=0.0015):
    """Expected P&L per transaction, expressed as a fraction of notional."""
    costs = np.array([0, 0.00001, 0.000024, 0.00003])
    expected = (2 * p - 1) * b - costs
    n = np.array([100, 1000, 10000, 100000])
    # Equicorrelation sensitivity, not a measured market correlation.
    effective = {str(rho): (n / (1 + (n - 1) * rho)).tolist()
                 for rho in [0, 0.001, 0.01]}
    return pd.DataFrame({'비용_bps': costs * 10000,
                         '거래당_기대손익_bps': expected * 10000}), n, effective

edge_table, breadth, effective = edge_example()
```

### 1.2.4 실제 계산 결과와 해석

| 거래당 비용 (bps) | 거래당 기대손익 (bps) |
|---:|---:|
| 0.00 | 0.24 |
| 0.10 | 0.14 |
| 0.24 | 0.00 |
| 0.30 | -0.06 |


대수의 법칙은 적절한 조건에서 표본 평균이 기대값에 가까워진다는 의미다. 유한한 기간의 손실이나 파산 위험이 사라진다는 뜻이 아니다. 거래 크기, 손익비, 상관관계, 비용을 동시에 바꿔 놓고 두 접근법의 우열을 평가해서도 안 된다. 몬테카를로 실험에서 손실 사건이 0회 관측되더라도 진짜 발생 확률이 0이라는 증거가 되지는 않는다.

## 1.3 금융 머신러닝의 세 가지 난제

### 1.3.1 낮은 신호대잡음비와 과적합

예측 가능한 부분보다 예측 불가능한 변동이 큰 문제에서는 훈련 성능이 쉽게 과장된다. 그러나 금융 데이터 전체의 잡음 비율을 하나의 고정 숫자로 제시할 수는 없다. 자산, 기간, 타깃과 정보 집합에 따라 달라지기 때문이다.

$$y_t=f(X_t)+\epsilon_t,\quad Cov(f(X_t),\epsilon_t)=0,\quad SNR=\frac{Var(f(X_t))}{Var(\epsilon_t)}$$

독립 잡음과 제곱오차를 가정하면, 고정된 입력에서 학습 표본에 대한 평균 예측 오차는 편향의 제곱, 추정량의 분산, 잡음 분산으로 분해된다.

$$E[(y-\hat f(X))^2]=Bias(\hat f(X))^2+Var(\hat f(X))+\sigma_\epsilon^2$$

이 식은 모델이 복잡할수록 언제나 실패한다는 법칙이 아니다. 모델의 용량과 표본 수를 고려해 검증해야 한다는 근거다. 정규화와 단순 기준 모형은 유용한 출발점이지만 좋은 데이터와 올바른 평가를 대신하지 않는다.

과거 이동평균을 ‘진짜 신호’로 놓고 잔차 분산과 비교하는 것만으로 실제 예측 SNR을 알 수는 없다. 이동평균도 잡음에서 만들어질 수 있다. 이번 실습에서는 생성 과정이 알려진 별도의 신호와 잡음에 대해서만 이론적 SNR을 계산한다.

### 1.3.2 비정상성과 ADF 검정

약정상 시계열은 평균과 유한한 분산이 시간에 따라 같고, 공분산이 시점 자체가 아닌 시차에만 의존한다. 가격 수준에는 추세나 단위근이 나타날 수 있으며, 수익률로 바꾸더라도 변동성 군집과 구조 변화가 남을 수 있다.

AR(1) 모형 yₜ=ρyₜ₋₁+εₜ에서 차분하면 Δyₜ=(ρ−1)yₜ₋₁+εₜ가 된다. ADF 검정은 여기에 상수·추세와 과거 차분을 추가해 단위근 귀무가설을 검정한다.

$$\Delta y_t=\alpha+\beta t+\delta y_{t-1}+\sum_{i=1}^{p}\gamma_i\Delta y_{t-i}+\epsilon_t$$

단위근 귀무가설은 δ=0이다. 단순 AR(1)의 정상 조건은 |ρ|<1이며 δ<0만으로 이 조건과 동치가 되지는 않는다. ADF 통계량은 일반적인 t 분포를 따르지 않는다. 임계값과 p값은 상수·추세 설정, 표본 수 등과 함께 해석한다.

실습은 상수만 포함하는 설정을 사용한다. 작은 p값은 그 설정에서 단위근 가설을 기각하는 근거이며, 모든 형태의 정상성이나 수익률의 예측 가능성을 증명하지 않는다. 큰 p값도 단위근이 확정되었다는 뜻은 아니다. [참고: statsmodels ADF 문서](https://www.statsmodels.org/dev/generated/statsmodels.tsa.stattools.adfuller.html).

![그림 1-3. 로그 가격과 수익률](assets/fig_01_03.png)

*그림 1-3. 이 합성 자료는 독립적인 로그 증분을 누적해 만들었다. 차분이 안정적으로 보이는 것은 생성 가정의 결과이며 실제 데이터 전반에 대한 증거가 아니다.*

### 1.3.3 재현 가능한 실습과 결과

```python
import numpy as np
import pandas as pd
from statsmodels.tsa.stattools import adfuller

def stationarity_example(n=1000, seed=42):
    rng = np.random.default_rng(seed)
    # Zero expected log increment. Differenced series is iid by construction.
    log_return = rng.normal(0, 0.012, n)
    log_price = np.log(100) + np.cumsum(log_return)
    results = []
    for name, series in [('로그 가격', log_price), ('로그 차분', np.diff(log_price))]:
        result = adfuller(series, regression='c', autolag='AIC')
        results.append({'시계열': name, 'ADF': result[0], 'p값': result[1],
                        '시차': result[2], '5%임계값': result[4]['5%']})
    # True signal is known only because we generate it explicitly.
    true_signal = rng.normal(0, 0.001, n)
    noise = rng.normal(0, 0.01, n)
    theoretical_snr = 0.001**2 / 0.01**2
    return log_price, log_return, pd.DataFrame(results), theoretical_snr

log_price, log_return, adf_table, snr = stationarity_example()
```

| 시계열 | ADF 통계량 | p값 | 사용 시차 | 5% 임계값 |
|---|---:|---:|---:|---:|
| 로그 가격 | -1.4074 | 0.5787 | 1 | -2.8644 |
| 로그 차분 | -28.9140 | 0 | 0 | -2.8644 |

별도로 정의한 합성 신호의 이론적 SNR은 0.01이다. 이는 실제 금융 데이터의 SNR 추정치가 아니다.

표에서 p값이 0으로 출력되면 수치 계산의 정밀도 안에서 매우 작게 처리되었다는 의미다. 귀무가설이 참일 확률이 정확히 0이라는 뜻은 아니다.

### 1.3.4 정보 누수·생존편향·반복 실험

| 문제 | 금융 데이터에서 생기는 상황 | 대응 |
|---|---|---|
| 미래참조편향 | 분기말에 아직 발표되지 않은 실적 반영 | 공시 발표 시점 이후에만 입력 사용 |
| 전처리 누수 | 전체 기간으로 평균·표준편차 계산 | 학습 구간에서만 전처리 통계 추정 |
| 라벨 구간 중첩 | 학습 라벨이 검증 시작 뒤 가격을 포함 | 라벨 종료 시점으로 경계 표본 제거 |
| 생존편향 | 현재 상장 종목만 과거로 추적 | 당시의 투자 가능 종목 집합 보존 |
| 사후 선택 편향 | 수백 전략 중 최고 성과만 제시 | 탐색 횟수 기록, 최종 평가 구간 분리 |

전처리와 학습을 함께 묶는 Pipeline은 학습 구간의 변환만 검증 구간에 적용하는 데 도움이 된다. 그러나 잘못된 타임스탬프, 미래 타깃을 포함한 입력, 잘못된 데이터 분할까지 자동으로 해결하지는 않는다. [참고: scikit-learn의 누수 방지 설명](https://scikit-learn.org/stable/common_pitfalls.html).

## 1.4 파이썬 금융 시계열 파이프라인 구축

### 1.4.1 관측 시점과 데이터 계약

데이터에는 값뿐 아니라 **언제 알 수 있었는가**가 포함되어야 한다. 최소한 관측 기준일, 발표·수신 시각, 종목 식별자, 수정 이력과 단위를 관리한다. 일별 데이터라면 거래소 달력과 시간대도 확인한다. 아래 실습의 영업일 인덱스는 월요일부터 금요일까지를 뜻하며 한국거래소 휴장일을 반영하지 않는다.

이 실습은 단일 자산의 액면분할과 라벨 정렬에 집중한다. 배당을 포함한 총수익률, 상장폐지, 거래정지, 거래량 보정, 체결 엔진까지 구현한 운영 시스템은 아니다.

### 1.4.2 수식: 가격 보정에서 타깃까지

액면분할일 k에 종가가 이미 분할 후 기준으로 기록되었다면, 과거 τ의 종가에는 **τ보다 뒤에 발생한 이벤트만** 적용한다. cₖ는 해당 이벤트의 가격 배율이고 이벤트가 없으면 1이다.

$$P_\tau^*=P_\tau\prod_{k=\tau+1}^{T}c_k$$

1주가 2주가 되는 분할에서는 cₖ=0.5다. 분할 전 50,000원을 분할 후 단위에 맞추면 25,000원이다. 과거 가격을 보정 계수로 나누거나 분할일 종가를 다시 보정하면 단층이 제거되지 않는다. 현금배당 보정은 이 단순 분할 공식에 그대로 대입하지 말고 공급자의 총수익률 정의를 확인해야 한다.

$$r_t=\log(P_t^*/P_{t-1}^*),\qquad R_t=\exp(r_t)-1$$

$$\log(P_T^*/P_0^*)=\sum_{t=1}^{T}r_t$$

로그 수익률은 시간에 대해 더할 수 있다. 여러 자산의 포트폴리오 가중합은 일반적으로 단순 수익률로 계산한다.

$$\bar r_t=\frac{1}{W}\sum_{i=0}^{W-1}r_{t-i},\qquad s_t=\sqrt{\frac{1}{W-1}\sum_{i=0}^{W-1}(r_{t-i}-\bar r_t)^2}$$

$$\sigma_t^{ann}=s_t\sqrt{252},\qquad y_t^{(h)}=\log(P_{t+h}^*/P_t^*)$$

252는 연간 거래일의 근사 관례다. 제곱근 시간 환산은 안정적인 분산과 낮은 자기상관 등의 가정이 필요하다. 또한 표본 분산의 불편성과 표본 표준편차 자체의 불편성을 혼동하지 않는다. 미래 타깃은 미래 **실현** 수익률이며, 조건부 기대수익률은 모델이 추정하는 대상이다.

![그림 1-4. 액면분할 보정](assets/fig_01_04.png)

*그림 1-4. 분할 전 가격에 0.5를 곱하면 가격 단위가 연결된다. 오른쪽은 분할 주변 로그 수익률이며, 보정 후에는 인위적인 약 −0.693의 단층이 사라진다.*

### 1.4.3 재현 가능한 실습

```python
import numpy as np
import pandas as pd

def financial_pipeline(n=500, window=20, horizon=5, seed=42):
    if n <= window + horizon or window < 2 or horizon < 1:
        raise ValueError('n, window, horizon의 범위를 확인하세요.')
    rng = np.random.default_rng(seed)
    dates = pd.bdate_range('2023-01-02', periods=n)
    increments = rng.normal((0.10 - 0.5 * 0.25**2) / 252, 0.25 / np.sqrt(252), n)
    base = 50000 * np.exp(np.cumsum(increments))
    raw = base.copy()
    split_day = n // 2
    raw[split_day:] *= 0.5
    df = pd.DataFrame({'raw_close': raw, 'split_factor': 1.0}, index=dates)
    df.iloc[split_day, df.columns.get_loc('split_factor')] = 0.5
    # Event-day close is already post-split; apply only strictly future factors.
    factors = df['split_factor'].shift(-1, fill_value=1)
    df['adjustment'] = factors.iloc[::-1].cumprod().iloc[::-1]
    df['adj_close'] = df['raw_close'] * df['adjustment']
    df['log_return'] = np.log(df['adj_close']).diff()
    df['vol_20d'] = df['log_return'].rolling(window).std(ddof=1) * np.sqrt(252)
    df['momentum_20d'] = df['log_return'].rolling(window).sum()
    df['target_fwd5d'] = np.log(df['adj_close'].shift(-horizon) / df['adj_close'])
    # Preserve unavailable labels instead of turning them into class 0.
    df['target_class'] = (df['target_fwd5d'] > 0).astype('Int64')
    df.loc[df['target_fwd5d'].isna(), 'target_class'] = pd.NA
    features = ['vol_20d', 'momentum_20d']
    training = df.dropna(subset=features + ['target_fwd5d']).copy()
    inference = df.dropna(subset=features).copy()
    assert np.allclose(df['adj_close'], base * 0.5)
    assert np.isclose(df['log_return'].iloc[split_day], increments[split_day])
    assert len(training) == n - window - horizon
    assert df['target_class'].tail(horizon).isna().all()
    return df, training, inference, features

raw, dataset, inference, features = financial_pipeline()
X = dataset[features]
y = dataset['target_fwd5d']
```

### 1.4.4 실제 결과와 시간 정렬

학습용 데이터는 **475행 × 9열**, 피처 행렬 X는 **475행 × 2열**이다. 최신 추론용 피처는 **480행**에 존재한다. 분할 보정의 연속성, 분할일 수익률, 학습 행 수, 마지막 타깃의 결측 보존을 코드의 검증문으로 확인했다.

피처는 20개의 수익률을 요구하므로 첫 20개 행을 사용할 수 없다. 학습용 데이터에서는 마지막 5개 행의 미래 타깃도 아직 알 수 없다. 따라서 500−20−5=475행이 남는다. 다만 **실제 추론에서는 마지막 5개 행도 유효한 입력**이다. 타깃이 없다는 이유로 최신 피처까지 버리면 안 된다.

현재까지 알려진 분할을 이용한 수정주가라도 과거 시점의 절대 가격 수준은 나중에 바뀔 수 있다. 실전 연구에서는 공급자의 수정 이력과 당시 사용 가능한 자료를 확인한다. 이 실습의 보정은 단위 연속성을 검증하는 예이며, 그 자체로 모든 시점 정보 관리가 해결되었다는 의미는 아니다.

## 1.5 시간 순서를 지키는 학습·검증 설계

![그림 1-5. 피처, 타깃, 검증 경계](assets/fig_01_05.png)

*그림 1-5. 위쪽은 한 샘플의 피처·미래 타깃 관계, 아래쪽은 학습·검증 경계의 개념도다. 두 줄은 같은 축척이 아니며 실제 경계는 타임스탬프로 계산한다.*

20일 모멘텀과 변동성은 t일까지의 데이터로 계산한다. 종가를 모두 확인한 후 신호를 만드는 경우 t일 종가에 그대로 체결되었다고 가정하지 않는다. 다음 거래 시점의 실행 가격과 비용을 정하고, 실제 거래 수익률 타깃은 그 정의에 맞추어 별도로 만든다. 이 장의 종가-종가 타깃은 예측 문제를 정의하기 위한 교육용 값이다.

분할은 과거 학습, 그 뒤 검증, 마지막 평가 구간 순서로 진행한다. h=5인 라벨을 사용한다면 학습의 마지막 샘플들이 검증 구간 가격을 포함할 수 있다. 예를 들어 첫 검증 입력이 관측일 300이면, 엄격한 비중첩 기준에서는 학습 라벨의 종료일 t+5가 300보다 앞서야 한다. 이 기준에서는 학습 입력의 마지막 날짜가 294다.

일정 간격의 단일 시계열에서는 5행의 간격으로 이를 구현할 수 있지만, 결측일과 여러 종목이 섞이면 행 개수만으로 처리하지 않는다. 각 라벨의 시작·종료 시각을 검사한다. 여러 검증 블록을 쓰는 설계에서는 중첩 라벨 제거와 추가 완충 구간의 필요성을 별도로 결정한다.

모델의 정확도와 전략의 수익률도 구분한다. 상승 빈도가 높은 데이터에서 항상 상승을 예측하는 분류기가 높은 정확도를 낼 수 있다. 반대로 작은 오차 개선이 거래비용을 넘는 경제적 개선을 만들지 못할 수도 있다. 다음 장에서는 기준 모형과 동일한 시계열 분할을 사용해 이 차이를 확인한다.

## 1.6 파이썬 머신러닝 생태계와 연구 환경

![그림 1-6. 파이썬 생태계](assets/fig_01_06.png)

*그림 1-6. 원본 PDF의 파이썬 생태계 주제(60–67쪽)를 금융 연구 단계와 연결해 재구성했다.*

| 도구 | 담당 역할 | 이 장 또는 다음 장의 사용 |
|---|---|---|
| NumPy | 배열, 수치 계산, 난수 | 재현 가능한 합성 데이터 생성 |
| pandas | 표, 날짜 인덱스, 이동창 계산 | 가격 보정과 피처·타깃 정렬 |
| Matplotlib | 그래프 | 분할 전후 가격, 자산 곡선 확인 |
| statsmodels | 통계 모형과 검정 | ADF 단위근 검정 |
| scikit-learn | 전처리, 학습, 평가 | 다음 장의 첫 기준 모델 |
| Jupyter | 코드·해설·출력의 결합 | 실험 과정의 재실행과 기록 |

파이썬 생태계를 익힌다는 것은 패키지 이름을 암기하는 것이 아니라 각 단계의 입력과 출력을 이해하는 것이다. 이 장에서는 코드가 만든 데이터셋을 다음 장의 학습 입력으로 전달한다.

동봉한 `requirements.txt`에는 이번 실행 환경의 패키지 버전을 기록했다. 아래 명령은 그 버전으로 실습을 재실행하는 예다.

```bash
python -m pip install -r requirements.txt
python chapter01_examples.py
```

그림, CSV, 결과 파일은 실행 스크립트와 같은 폴더에 생성된다. 그림은 맑은 고딕 글꼴을 사용하므로 다른 운영체제에서는 설치된 한글 글꼴로 바꾸면 된다. 난수 시드를 고정해도 라이브러리 버전에 따라 통계 검정의 마지막 자리에는 차이가 있을 수 있다.

## 1.7 장 요약과 확인 문제

규칙은 명시적인 통제에, 학습은 데이터 관계 추정에 활용할 수 있다. 금융 연구에서는 작은 통계적 우위가 비용 후에도 남는지 확인하고, 시간 순서와 당시 이용 가능한 정보를 유지해야 한다. 데이터 변환은 수식과 코드가 일치해야 하며, 성과 수치는 실행한 결과로만 보고해야 한다.

### 확인 문제

1. 5개 변수를 각각 4구간으로 나누면 가능한 상태 수는 얼마인가? 이것이 모든 모델의 필수 학습 표본 수인가?
2. 승률 50.8%, 이익·손실 크기 0.15%인 거래의 비용 전 기대손익은 몇 bps인가?
3. ADF p값이 0.01이면 수익률의 예측 가능성이 증명되는가?
4. 1주가 2주가 되는 분할에서 과거 가격에는 어떤 계수를 적용하는가? 분할 당일도 보정하는가?
5. 왜 최신 5일 데이터가 학습에서는 빠지지만 추론에서는 남을 수 있는가?
6. 현재 시점의 우량 종목 목록으로 10년 전 전략을 검증하면 어떤 편향이 생길 수 있는가?

### 해설

1. 4⁵=1,024개다. 이는 격자 상태 수이며 모든 모델의 보편적 표본 수 하한이 아니다.
2. (2×0.508−1)×0.0015×10,000=0.24bps다.
3. 아니다. 해당 설정에서 단위근 귀무가설을 기각하는 근거다.
4. 과거 가격에 0.5를 곱한다. 이미 분할 후 기준인 당일 종가는 다시 보정하지 않는다.
5. 과거 기반 피처는 존재하지만 미래 실현 타깃이 아직 없기 때문이다.
6. 사라진 기업을 누락하는 생존편향과 당시 구성 종목 정보를 무시하는 문제가 생길 수 있다.

### 확장 실습

실습 1의 비용을 0, 0.0003, 0.001로 바꿔 거래비용 민감도를 비교한다. 실습 4의 관측일 수를 600, 예측 기간을 10으로 바꾸고 정제 행 수를 예측한 뒤 확인한다. 수정주가와 합성 원본 가격의 배율 관계가 전체 구간에서 유지되는지 검증한다. 실습 4의 열 이름에 있는 20·5는 기본 설정을 표현하므로 기간을 바꾸는 응용에서는 열 이름도 함께 바꾼다.

## 다음 장으로 연결하기

**Chapter 02 예정 주제: 사이킷런으로 시작하는 금융 머신러닝.** 이번 장에서 만든 `vol_20d`와 `momentum_20d`를 입력으로, `target_fwd5d`를 회귀 타깃으로 사용한다. 단순 기준 예측, 시계열 분할, 학습 구간 전처리, 첫 모델과 평가를 차례로 추가할 수 있다. 이는 확장 방향 제안이며 다음 장 본문은 아직 작성하지 않았다.

각 장은 ‘학습 목표 → 이전 장 연결 → 개념과 수식 → 그림 → 실행 코드 → 실제 결과 → 해석 → 연습문제 → 다음 장’의 같은 순서를 유지한다. 장별 그림 번호는 1-1, 2-1처럼 별도로 시작해 다른 장이 추가되어도 기존 참조가 흔들리지 않도록 한다.

## 출처와 편집 기록

| 자료 | 반영 범위 |
|---|---|
| 첨부 *Machine Learning with Python*, PDF 3–67쪽 | 머신러닝 정의, 학습 유형, 피처·레이블, 학습·예측, 파이썬 생태계 |
| 사용자 제공 Chapter 01 초안 | 1.1–1.4의 금융 주제와 개념·수식·코드·결과·해석 구조 |
| statsmodels ADF 공식 문서 | 검정의 반환값과 해석 |
| scikit-learn Common pitfalls 공식 문서 | 학습·검증 분리와 전처리 누수 방지 |
| SEC 투자자 교육 자료 | 비용이 투자 성과에 미치는 영향 |

금융 적용 수식과 합성 실험은 이 장의 추가 설명이며 원본 PDF에 같은 내용이 실려 있다는 뜻은 아니다. 참고 링크는 각 관련 절에 배치했다. 외부 자료의 확인일은 2026년 9월 4일이다.

**주요 수정 사항**: 실행 불가능한 배열 구문과 ADF 반환값 인덱싱 수정; 모사 출력 대신 실제 계산값 반영; 분할 보정의 방향·경계 수정; 포트폴리오 복리와 최대낙폭 계산 정리; 학습용 결측 타깃과 추론용 최신 피처 분리; 근거 없는 고정 잡음 비율·무위험 수렴·펀드 내부 전략 단정 제거. EMH·CAPM·GBM을 동일한 가정 체계로 묶는 서술과 특정 사건으로 모든 규칙 기반 전략의 실패를 단정하는 설명도 삭제했다.
